
from enum import Enum

class SeatsStatus(Enum):
    AVAILABLE = "available"
    RESERVED = "reserved"

class BookStatus(Enum):
    USED = "used"
    NEW = "new"

class EmployeeRole(Enum):
    ADMIN = "used"
    REGULAR = "new"
    CASHER = "new"